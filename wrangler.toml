# Cloudflare Pages + Functions
name = "zzsky-site"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"

# Optional: after creating KV in Dashboard, uncomment and paste the namespace id
# (Dashboard binding is still required for Pages Production/Preview)
# [[kv_namespaces]]
# binding = "QUOTES_KV"
# id = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

[vars]
# Non-secret config can go here; put secrets in Dashboard Secrets / wrangler pages secret
# ALLOWED_ORIGINS = "https://www.zzsky.com,http://localhost:5173"
# EMAIL_FROM = "ZZSKY Quote <noreply@your-verified-domain.com>"
# NOTIFY_EMAIL = "sales@aorelaser.cn"
# CUSTOMER_AUTO_REPLY = "true"
# RATE_LIMIT = "5"
